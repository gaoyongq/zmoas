package com.zm.mall.service;

import com.alibaba.fastjson.JSONObject;
import com.zm.mall.domain.business.product.RegionCatDto;
import netscape.javascript.JSObject;

import java.util.HashMap;
import java.util.Map;

/**
 * Created by Bochao on 2017/4/12.
 */
public class TestJson {
    public static void main(String[] args) {
        Map<RegionCatDto, RegionCatDto> map = new HashMap<RegionCatDto, RegionCatDto>();
        map.put(new RegionCatDto(), new RegionCatDto());
        System.out.println(JSONObject.toJSONString(map));
    }
}
